name: Sync to Hugging Face Space

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  sync-to-hf:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout GitHub repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check HF_TOKEN
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          if [ -z "$HF_TOKEN" ]; then
            echo "HF_TOKEN is empty. Please add it in GitHub Settings -> Secrets and variables -> Actions."
            exit 1
          fi
          echo "HF_TOKEN exists."

      - name: Push to Hugging Face Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          set -e

          git config --global user.email "github-actions@github.com"
          git config --global user.name "github-actions"

          rm -rf /tmp/hf-space

          git clone https://1798160098-byte:${HF_TOKEN}@huggingface.co/spaces/1798160098-byte/astock-research-api /tmp/hf-space

          rsync -av --delete \
            --exclude ".git" \
            --exclude ".github" \
            --exclude "__pycache__" \
            --exclude ".env" \
            --exclude ".env.*" \
            --exclude ".DS_Store" \
            --exclude "*.log" \
            ./ /tmp/hf-space/

          cd /tmp/hf-space

          git add -A
          git status

          git commit -m "Sync from GitHub ${GITHUB_SHA}" || echo "No changes to commit"
          git push origin main
